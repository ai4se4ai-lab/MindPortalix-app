# Styling Rules

- Use CSS custom properties for color and spacing tokens.
- Keep the MVP UI accessible without a build step.
- Ensure chat content wraps and remains readable on mobile.
- Prefer responsive grids over fixed desktop-only layouts.
