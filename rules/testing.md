# Testing Rules

- Put agent unit tests under `tests/agents`.
- Put end-to-end orchestration tests under `tests/integration`.
- Put hook policy tests under `tests/hooks`.
- Use fixtures for reusable prompts and expected routes.
- Test privacy and failure paths, not only successful responses.
