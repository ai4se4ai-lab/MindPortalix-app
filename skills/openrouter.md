# OpenRouter Skill

Use `src/openrouter/client.js` to build chat completion requests. Prefer free MVP models from the registry and keep temperature low for orchestration, review, and memory compression.

Required environment:
- `OPENROUTER_API_KEY`
- `OPENROUTER_BASE_URL`
- `APP_URL`
