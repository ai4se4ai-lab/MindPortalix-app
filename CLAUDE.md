# MindPortalix Project Context

MindPortalix orchestrates specialist agents on free OpenRouter models. Keep changes aligned with the agent registry, reviewer validation, memory privacy, and Supabase RLS requirements.

Run `npm test` after code changes.
